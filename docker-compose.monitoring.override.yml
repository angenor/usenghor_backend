version: "3.8"

services:
  prometheus:
    ports:
      - "127.0.0.1:9090:9090"

  grafana:
    ports:
      - "127.0.0.1:3001:3000"
    environment:
      GF_SECURITY_COOKIE_SECURE: "false"
      GF_SERVER_ROOT_URL: "http://localhost:3001"
      GF_SERVER_DOMAIN: "localhost"
